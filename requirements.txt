# ===== 코어 =====
# Gemini (무료!) ⭐ - 최신 SDK 사용
google-genai>=0.2.0

# Claude (선택)
anthropic>=0.40.0

# YouTube
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
yt-dlp>=2025.12.0  # YouTube 영상 다운로드 (리믹스 시스템)
pysrt>=1.1.2  # SRT 자막 파싱/편집

# ===== 비디오 처리 =====
moviepy>=2.0.0  # MoviePy 2.x (최신 API)
imageio-ffmpeg>=0.5.0  # FFmpeg for MoviePy (필수!)
ffmpeg-python>=0.2.0
pydub>=0.25.1

# ===== OCR (하드코딩 자막 추출) =====
easyocr==1.7.2  # OCR 엔진
opencv-python-headless==4.12.0.88  # 영상 처리 (headless 버전)
numpy>=2.3.0  # 배열 연산 (Python 3.14 호환)
# torch와 torchvision은 아래 명령으로 설치:
# pip install --only-binary :all: torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch==2.9.1  # PyTorch CPU 버전
torchvision==0.24.1  # torchvision CPU 버전
scipy==1.16.3
scikit-image==0.26.0
PyYAML==6.0.3
Shapely==2.1.2
pyclipper==1.4.0
python-bidi==0.6.7
ninja==1.13.0

# ===== TTS =====
gTTS>=2.5.0
google-cloud-texttospeech>=2.17.0
elevenlabs>=0.2.27
pyttsx3>=2.90

# ===== 이미지 생성 (선택) =====
# stability-sdk>=0.8.4  # 선택적
# diffusers>=0.27.0  # 로컬 SD 사용 시
# torch>=2.1.0

# ===== 유틸리티 =====
click>=8.1.0
python-dotenv>=1.0.0
pillow>=11.0.0  # Python 3.14 지원
requests>=2.32.0
pydantic>=2.0.0  # 데이터 모델
schedule>=1.2.0  # 스케줄링
psutil>=6.0.0  # 시스템 리소스 모니터링 (벤치마크용)

# ===== 데이터베이스 (Phase 1) =====
sqlalchemy>=2.0.23
alembic>=1.13.1
pydantic-settings>=2.1.0

# ===== 서버 (선택) =====
fastapi>=0.115.0
uvicorn>=0.32.0
